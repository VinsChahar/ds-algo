/* 1. using for loop */

function myReduce(calback) {
    const newArray = [];

    for (let i = 0; i < array.length; i++) {
        const element = array[i];

    }

    return newArray;
}


/* 2. using foreach loop */
function myReduceForEach(calback) {
    this.forEach(() => { });

}
